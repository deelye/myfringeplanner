<div class="container">
  <div class="show-container">

    <%= link_to "#{@show.website}", target: :blank do %>
      <div class="polaroid">
        <%= image_tag "#{@show.original_image}", alt: "#{@show.title}" %>
        <h3><%= @show.artist %></h3>
      </div>
    <% end %>

    <div class="show-calendar">
      <%= FringeCalendar.new(self, {}).render do |date| %>
        <%= date.day %>
      <% end %>
    </div>


  </div>
</div>




